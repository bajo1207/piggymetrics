spring:
  application:
    name: monitoring
  cloud:
    config:
      uri: http://ip-10-0-0-123.ec2.internal:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user